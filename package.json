{
  "name": "qr-generator",
  "version": "1.0.0",
  "description": "Modern QR Code Generator with Text and Image support",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["qr-code", "generator", "full-stack"],
  "author": "Aadi",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

